# Dependencies: not ours to format.
node_modules/

# HTML stays out on purpose, mirroring the app: the calculator page carries the
# ?v=N cache-busting on every import, and reflowing those tags buys nothing while
# risking the one thing the browser is picky about. Revisit alongside the folder
# reorganisation.
*.html

# Project docs follow a one-line-per-item rule that Prettier would rewrap.
*.md

# Generated by npm, and rewritten on every install.
package-lock.json
